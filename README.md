# Media3Watch

Minimal Jetpack Compose screen that plays Big Buck Bunny using Media3 ExoPlayer and `PlayerSurface`.

## Run

Open the project in Android Studio and run the `app` configuration on a device or emulator.

